# Skills

Repository-local skills should be stored as:

`.agents/skills/<skill-name>/SKILL.md`

If cross-tool compatibility is needed, mirror or reference the same skill from:

`.claude/skills/<skill-name>/SKILL.md`
